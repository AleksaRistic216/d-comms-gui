Build the project and launch $ARGUMENTS isolated dui instances in parallel (default: 2).

Run the following command and stream its output:

```bash
./run_instances.sh $ARGUMENTS
```

Remind the user that closing all windows will automatically clean up the temp directories.
